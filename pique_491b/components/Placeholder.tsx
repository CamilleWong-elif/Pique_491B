import { View } from 'react-native';

// These are temporary stubs that will be replaced as each component is converted.
// They render nothing but allow HomePage to compile without errors.

export function EventCard(_props: any) {
  return <View />;
}

export function SocialActivityCard(_props: any) {
  return <View />;
}

export function SearchOverlay(_props: any) {
  return <View />;
}

export function NotificationsModal(_props: any) {
  return <View />;
}