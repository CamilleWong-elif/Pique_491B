node_modules/

# Local env (secrets)
.env
.env.*

# Firebase service account key (never commit — use env vars on Railway)
*.json
!package.json
!package-lock.json

# Runtime / tooling
*.log
npm-debug.log*
yarn-debug.log*
coverage/
.nyc_output/
